name: Extract Shot Data

on:
  workflow_dispatch:        # Allow manual runs from the Actions tab

jobs:
  extract:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Scrape result URLs
        run: python scrape_results.py --output-dir output

      - name: Extract shot data
        run: python extract_shot_data.py --output-dir output

      - name: Commit and push updated data files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: update shot data [skip ci]"
            git push
          fi

      - name: Upload output as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: shot-data-${{ github.run_id }}
          path: output/
          retention-days: 90
